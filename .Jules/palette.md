## 2024-03-10 - ARIA Labels in PlayerBar
**Learning:** The PlayerBar desktop volume control and mobile expansion controls are often implemented using bare `<button>` or `<div>` elements without proper accessibility roles, titles, or aria-labels.
**Action:** Always verify that every interactive button and progress bar contains appropriate `aria-label`, `title`, and keyboard handlers for screen reader compatibility.
